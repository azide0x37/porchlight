[project]
name = "porchlight"
version = "2.3.0"
description = "LAN directory appliance with Home Assistant MQTT bridge"
requires-python = ">=3.11"

[tool.pyright]
pythonVersion = "3.11"

[tool.ruff]
line-length = 120
